namespace Week1_Introduction
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //class is the blueprint; it cannot exist in memory; it doesnt allow assignments
            //out of the class we can create an object, which will allow us to assign values

            Person p = new Person(); //creating an object
            p.Name = "Test";


            

        }
    }
}